export type PaginatorOutput = {
  pagesCount: number; // Total number of available pages
  page: number; // Current page number
  pageSize: number; // Number of elements on the page
  totalCount: number; // Total number of elements
};
