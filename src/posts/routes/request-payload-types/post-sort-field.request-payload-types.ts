export enum PostSortFieldRP {
  CreatedAt = "createdAt",
  Title = "title",
  ShortDescription = "shortDescription",
  Content = "content",
  BlogName = "blogName",
}

export enum PostCommentsSortFieldRP {
  CreatedAt = "createdAt",
}
