export enum PostSortField {
  CreatedAt = "createdAt",
  Title = "title",
  ShortDescription = "shortDescription",
  Content = "content",
  BlogName = "blogName",
}

export enum PostCommentsSortField {
  CreatedAt = "createdAt",
}
